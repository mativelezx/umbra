# Umbra: interfaz de reflexión inspirada en Stoic

> For agentic workers: use subagent-driven-development. User approved code-first on 2026-09-06; do not request another visual direction gate.

Goal: replace the cosmic presentation with an original Umbra interface, a dark fluid illustrated landing page, light task-centered interiors and a working local example journey for browser review.

Implementation record: source is complete through `c9bf16a`. The checklist below preserves the original light-only brief as historical planning evidence; subsequent explicit user requests supersede its font, illustration and landing prescriptions. The final direction is lowercase `umbra`, Bricolage Grotesque, four original SVG task scenes, a dark animated landing page and contrasted Stoic-inspired patterns throughout. Final verification and residual scope are recorded in `docs/DEMO-UI-VERIFICACION-2026-09-06.md`; visual tokens belong to root `DESIGN.md`.

Architecture: preserve the existing Next.js App Router, React, Tailwind, Phosphor components, routes, state and API contracts. One coherent implementation task owns the shared visual change; independent review and browser QA follow it. No new design framework.

Tech stack: actual candidate Next.js 15.5.18, React 18, Tailwind 3.4, Vitest, Playwright.

Spec: `plans/BRANDING-DEMO-UMBRA-2026-09-06.md` and `PRODUCT.md`.

## Global constraints

- Preserve name Umbra and existing functionality. Original light graphite/white identity inspired by Stoic principles, no copied assets, bird, text or exact layout.
- Spanish with voseo, plain operational navigation: Mi resultado, Actividades, Chat, Informe; account/settings remain reachable.
- Desktop and mobile web; public and authenticated surfaces; verification data mode is explicitly synthetic local demo. No production writes, external AI charges, deploy, DB/schema/auth/privacy/safety/prompt/ML changes.
- No invented accuracy, individual certainty, clinical claims or human usability evidence. Show Big Five as experimental ML, Jung as AI interpretation, activities as reflection suggestions. Remove certainty percentages from presentation, including quick glance and export, not model schema.
- Preserve external failure visibility and retry behavior. Never make a fixture action appear to persist or call a live generation endpoint.
- No new dependencies or architectural migration. Use the existing UI primitives, icons and one self-hosted readable sans. Keep semantic state colors accessible but sparse.
- Avoid tiny uppercase/monospaced labels, italic hero headlines, decorative glow, noise, orbs, purple gradients, nested card scaffolding. Accessible focus, body contrast >=4.5:1, reduced motion, 44px touch controls where appropriate.
- Work only in `/private/tmp/umbra-entrega-audit.B6Tyg8/repo`, branch `codex/stoic-demo`, based on c3fff06. Original root and its ML edits remain untouched. Baseline generated `next-env.d.ts` belongs to prior QA; do not include it in product commits.
- No promise of complete live demo readiness, approval CAE, authorship concealment, or detector passing. Visual review and live integration are distinct.

## Context and decision record

Decision UI-STOIC-2026-09-06: approved direction, implementation pending. Actor: adult Spanish-speaking learner and student presenter. Job: answer -> understand experimental result -> choose activity -> export. Why now: urgent academic re-delivery and video preparation. Success: visible route clarity, readable question/result, no dishonest demo controls, no runtime/axe failures in scoped example checks. Failure: lost text, unreadable state, broken navigation or external request from fixture action.

Existing APIs/stores/types unchanged except local presentational props. No new events, cost, providers, data recipients, legal claims or infrastructure. Existing privacy and crisis handling remain in place. Reference research already completed on Mobbin read-only. Shared foundation: Tailwind tokens, globals, root layout, UI components and app shell. Rebrand approval is explicit. Remaining external gate: actual Supabase/ML/Anthropic integration and delivery approval.

Dogfood scenario: on localhost with synthetic fixtures, desktop and mobile, open landing -> onboarding example -> results -> activity checkbox -> report; inspect errors and keyboard focus. Capture screenshots, console/requests and axe. Never submit personal data or send generation to external providers. Stop on external mutation or privacy/auth boundary change. Docs updated to observed built state after verification.

## Task 1: Implement the coherent UI and honest interaction states

Files: `tailwind.config.ts`, `app/globals.css`, `app/layout.tsx`; `components/ui/*`, `components/layout/*`; `app/page.tsx`, `app/onboarding/page.tsx`, `app/dashboard/page.tsx`, `app/plan/page.tsx`, `app/export/page.tsx`, `app/export/print.css`; their presentational components under `components/onboarding/` and `components/dashboard/`; existing auth/chat/settings presentation where required for shared theme consistency. Add focused tests alongside modified behavioral components. Do not change API routes, models, prompts, safety, migrations or backend services.

- [ ] Read the spec, product and existing source. Load Impeccable craft floor immediately before UI edits; user pinned Stoic overrides random design exploration. Do not rerun design context/seed or seek new approval.
- [ ] Add rendering/interaction regression tests before behavior changes, run RED with actual failure evidence. Tests must render real components with external boundaries mocked only when necessary, not grep source or assert copied constants.
- [ ] Cover NarrativeSection failure preserving prior text and a working retry; handle HTTP and explicit streamed failure without dropping saved content. Cover initial failure retry. For synthetic `demo-` profile, regeneration must not call fetch and a truthful example state must be visible.
- [ ] Cover result presentation with an 82 confidence fixture: no rendered precision/certainty claim or progressbar labeled certainty; experimental source and limits remain readable. Preserve unsupported-dimension states. Use role/text semantics, not CSS values as tests.
- [ ] Apply one original light brand: grayscale surfaces and own geometric U mark, short bold sans headings, readable body width, limited purposeful containers. Prefer Next font self-hosted Inter already used; no new external runtime font links. No illustrated mascot required.
- [ ] Simplify root decoration and style primitives for all states, consistent layout, visible keyboard focus and reduced motion. Replace dark-only hardcoded colors in affected consumers rather than stacking blanket CSS overrides. Legacy token compatibility may remain documented if necessary to avoid unrelated feature refactors.
- [ ] Navigation uses approved Spanish labels and same route targets; settings/profile still accessible. Active state includes aria-current and is not only color. Mobile controls fit without horizontal scrolling.
- [ ] Onboarding keeps existing modes and state transitions: one question, clear instructions, visible selection and verifiable progress. Label Carta field and explain word threshold. Demo back button must either work locally or clearly be unavailable; no inert control. Preserve answers on recoverable failures.
- [ ] Results lead with readable context/interpretation and limits; supporting technical data is secondary. Remove individual confidence displays and unsupported population comparisons; do not hide experimental caveats in a foldout. Fix archetype contrast without altering scientific semantics.
- [ ] Activities keep the actual flow and progress behavior; synthetic checkbox state says local, not stored. Prevent fixture regeneration from touching live APIs. Chat may remain authenticating but must be honest, not a fake working chat.
- [ ] Export follows the same reading system and source/limit labels, with consistent print styles. Do not rework account data export/privacy contracts.
- [ ] Record direction contract in root layout: direction29378ec1, Operate, light reflection journal; U mark, sans, grayscale; whitespace/typographic hierarchy; no clinical/precision claims; no cosmic legacy; snapshots across desktop/mobile. Use ordinary source comment and stable body data attribute rather than custom rendering machinery solely to emit an HTML comment (framework adaptation, not product behavior).
- [ ] Run focused GREEN, then once full `pnpm test`, `pnpm typecheck`, `pnpm lint`. Self-review and commit scoped source/tests. Do not stage unrelated generated next-env.
- [ ] Write task report including RED/GREEN output, changed files, commit and limitations in the SDD workspace. No nested subagents; controller owns external review, browser QA and design documentation.

Example behavioral regression shape (adapt exact accessible labels to the real UI):

```tsx
render(<NarrativeSection profileId="profile-test" initialContent="Mi texto anterior" />);
fireEvent.click(screen.getByRole('button', { name: /regenerar|volver a generar/i }));
await screen.findByRole('alert');
expect(screen.getByText('Mi texto anterior')).toBeVisible();
expect(screen.getByRole('button', { name: /reintentar|regenerar|volver a generar/i })).toBeEnabled();
```

The external fetch returns a failed HTTP response for this case. A second test returns an explicit failed SSE event. A third renders a demo profile and verifies that no external fetch occurs. Tests should kill the current clear-before-request behavior, not just assert the mocked error response.

## Controller verification and handoff

- [ ] Review Task 1 diff independently for spec/quality; address blockers within bounded fixes.
- [ ] Build/start with local placeholder provider configuration plus explicit demo flag, not original credentials. Run public E2E and scoped example interactions/axe, desktop/mobile/user viewport screenshots, at most two batched visual rounds before fresh finish review.
- [ ] Run Impeccable detector once on changed UI files; adjudicate findings against pinned world. Fresh independent finish reviewer checks screenshots and implementation. Fix real blockers and record residual scope.
- [ ] Documenter writes `DESIGN.md` from verified code and marks/replaces outdated cosmic `docs/DESIGN_SYSTEM.md`. Update approval/verification record with actual evidence and demo limitations.
- [ ] Keep local preview available and open in user's browser. Preserve isolated branch; no automatic merge, push or deploy. Report exact path, tests, and pending real-services checks.

## Preflight

Baseline 2026-09-06: isolated candidate c3fff06, Vitest 112/112 in 12 files passed before UI edits. Current original root beta differs and has user ML changes. Prior candidate static/public E2E evidence does not prove this new interface or real provider integration.
